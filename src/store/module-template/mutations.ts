import { MutationTree } from "vuex";
import { ModuleState } from "./state";

const mutation: MutationTree<ModuleState> = {
  // setCategories(state: ModuleState, payload: Category[]) {},
};

export default mutation;
